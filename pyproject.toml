[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-harness-sample"
version = "0.1.0"
description = "Skeleton for an agent system with harness engineering"
requires-python = ">=3.11"

[tool.setuptools]
packages = []
